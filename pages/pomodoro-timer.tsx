import type { NextPage } from 'next';

import { PomodoroTimerTemplate } from '../src/components/features/pomodoro-timer/PomodoroTimerTemplate';

const PomodoroTimerPage: NextPage = () => {
  return <PomodoroTimerTemplate />;
};

export default PomodoroTimerPage;
